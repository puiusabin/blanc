{
  "name": "@blanc/mail-server",
  "version": "0.1.0",
  "private": true,
  "description": "Haraka mail server with PostgreSQL and wallet authentication",
  "main": "index.js",
  "scripts": {
    "start": "haraka -c /app/config",
    "dev": "haraka -c ./config",
    "clean": "rm -rf queue/ logs/"
  },
  "dependencies": {
    "@blanc/database": "*",
    "Haraka": "^3.1.1",
    "pg": "^8.11.3",
    "openpgp": "^5.11.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}